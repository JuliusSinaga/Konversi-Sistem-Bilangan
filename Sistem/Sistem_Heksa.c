#include "Sistem.h"
#include <stdio.h>
#include <string.h>

// Untuk melakukan konversi bilangan desimal ke heksadesimal dalam bahasa C, kita bisa menggunakan format %x untuk heksadesimal
void konversi_heksa(long long _bilangan) {
    printf("Dalam sistem bilangan Heksadesimal : %llx", _bilangan);
}